# Besu Deployment

## Besu native

## Besu container

**For run simple dev node enter in `/besu-deployment/container/dev` and run**

```bash
./run.sh
```

**For simulating DREX with PoA consensus enter in `/besu-deployment/container/network` and run**

```bash
docker compose up
```
